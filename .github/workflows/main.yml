name: cd
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on:
  workflow_dispatch:
jobs:
  Update-Image:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    - run: echo "you are in $PWD"
    - run: sed -i '$s/.*/  newTag: "10"/g' kustomize/overlays/dev/kustomization.yml
        
        
        
